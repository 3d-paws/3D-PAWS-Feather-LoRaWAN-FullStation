#
# CONFIG.TXT
#
# Line Length is limited to 60 characters

# LoRaWAN ABP (Authentication By Personalization)
# Input must be hex and match the lengths below
lw_dev_addr=
lw_nwk_skey=
lw_app_skey=

# 1 minute observation period is the default
# 5 minute observation periods. If set overrides 15m and 1m options
# Options 0,1
5m_enable=0

# 15 minute observation periods. If set overrides 1m options
# Options 0,1

15m_enable=0

# Rain Gauge rg1 pin A2
# Options 0,1
rg1_enable=1

# Rain Gauge rg2 pin A3
# Options 0,1
rg2_enable=0

# Distance Sensor for Snow, Surge, Stream on pin A5
# Options 0 = No sensor, 5 = 5M sensor, 10 = 10m sensor
ds_enable=0

# Number of hours between daily reboots
# A value of 0 disables this feature
daily_reboot=22
